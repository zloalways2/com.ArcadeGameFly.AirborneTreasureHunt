using System;
using UnityEngine;
using ZeroSDK.UIBuilder.AddOns.Button;
using ZeroSDK.UIBuilder.Core;
using ZeroSDK.UIBuilder.Core.UIElements;

namespace _project.Scripts
{
    public sealed class herojherpjherpoh : rereherherherh
    {
        [SerializeField] private rewgrehtrn policyButton;
        [SerializeField] private rewgrehtrn playButton;
        [SerializeField] private rewgrehtrn optionsButton;
        [SerializeField] private rewgrehtrn exitButton;
        
        public event Action hjerejhoerihjerohij
        {
            add => playButton.oerherh += value;
            remove => playButton.oerherh += value;
        }
        
        public event Action grkeothgdflsfd
        {
            add => optionsButton.oerherh += value;
            remove => optionsButton.oerherh += value;
        }
        
        public event Action poerhjrephjo
        {
            add => exitButton.oerherh += value;
            remove => exitButton.oerherh += value;
        }
        
        public event Action rogpjerhopjerhojerh
        {
            add => policyButton.oerherh += value;
            remove => policyButton.oerherh += value;
        }


        protected override void pijerhpjrehperjh() => prgojerhpojerhpjer.ewregtrh.rjhjoerhjerpohj<rgpoejrrhojerhj>().reherherherhe();
    }
}